# grafquery

Install the `grafquery` CLI from npm.

## Install

```bash
npm install -g grafquery
```

The installer downloads the platform binary from GitHub Releases for the matching package version, installs it into `~/.local/bin`, and attempts to update your user `PATH` automatically.
